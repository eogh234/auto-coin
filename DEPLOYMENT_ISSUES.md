# 🚨 배포 실패 해결 가이드

## 현재 문제점들

### 1. SSH 키 인증 실패

- GitHub Secrets의 `DEPLOY_SSH_KEY`가 올바르지 않음
- 서버 접속 시 `Permission denied (publickey)` 오류

### 2. 테스트 환경 문제

- CI 환경에서 `config.yaml` 파일이 없음
- 모듈 초기화 파라미터 불일치

### 3. YAML 모듈 호환성

- `ensure_ascii` 파라미터 지원하지 않는 YAML 버전

## 🔧 해결 방법

### 1단계: GitHub Secrets 설정

1. **GitHub 레포지토리 → Settings → Secrets and variables → Actions**로 이동

2. **DEPLOY_SSH_KEY 설정**:

```bash
# 개인키 내용 복사 (전체 내용)
cat ~/.ssh/id_ed25519
```

3. **SERVER_IP 설정**:

```
152.70.39.62
```

### 2단계: SSH 키 형식 확인

```bash
# SSH 키 헤더/푸터 확인
head -1 ~/.ssh/id_ed25519
tail -1 ~/.ssh/id_ed25519
```

올바른 형식:

```
-----BEGIN OPENSSH PRIVATE KEY-----
[키 내용]
-----END OPENSSH PRIVATE KEY-----
```

### 3단계: 서버 연결 테스트

```bash
# 로컬에서 SSH 연결 테스트
ssh -i ~/.ssh/id_ed25519 ubuntu@152.70.39.62 'echo "연결 성공"'
```

## 📋 즉시 수행할 작업들

1. ✅ SSH 키 파일 존재 확인됨
2. ⏳ GitHub Secrets에 올바른 SSH 키 설정 필요
3. ⏳ 테스트 파일 수정 필요
4. ⏳ YAML 모듈 호환성 수정 필요

## 🚀 다음 단계

1. GitHub Secrets 업데이트
2. 테스트 파일 수정
3. 재배포 실행
